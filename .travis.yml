language: go

go:
  - 1.2
  - 1.3
  - 1.4.3
  - tip

os:
  - linux
  - osx
  
before_install:
  - if [ ${TRAVIS_OS_NAME} == "linux" ]; then sudo apt-get update -q -y; fi
  - if [ ${TRAVIS_OS_NAME} == "linux" ]; then sudo apt-get install libnetcdf-dev -y; fi
  - if [ ${TRAVIS_OS_NAME} == "osx" ]; then brew tap homebrew/science; fi
  - if [ ${TRAVIS_OS_NAME} == "osx" ]; then brew update; fi
  - if [ ${TRAVIS_OS_NAME} == "osx" ]; then brew install netcdf; fi
  - go get github.com/fhs/go-netcdf/netcdf
  - go get github.com/pborman/getopt
  
install :
  - go install -v ../Oceano2oceansitesTest/toml
  - go install -v ../Oceano2oceansitesTest/roscop
  - go install -v ../Oceano2oceansitesTest/lib
  - go install -v ../Oceano2oceansitesTest/config
  - go install -v ../Oceano2oceansitesTest/analyse
  - go install -v ../Oceano2oceansitesTest/netcdf
  - go install -v ../Oceano2oceansitesTest/mk21
  - go install -v ../Oceano2oceansitesTest/seabird
  - go install -v ../Oceano2oceansitesTest/thecsas
  - go install -v ../Oceano2oceansitesTest/ifm
 
script:
  - go build -i
